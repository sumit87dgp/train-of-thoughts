# Frontend environment template (safe to commit)
#
# Copy to .env (never commit .env):
#   cp .env.example .env
#
# Vite exposes only VITE_* variables to the client (import.meta.env).
# No trailing slash on the API URL.

VITE_API_URL=http://localhost:8000
